/**
 * Capa de controladores (Controller).
 * <p>
 * Expone los endpoints REST que consume el frontend. Se encarga de recibir
 * las peticiones HTTP, delegar la lógica a la capa {@code service} y devolver
 * las respuestas (DTOs) correspondientes. No debe contener lógica de negocio
 * ni acceso directo a la base de datos.
 */
package com.sena.multicapa.controller;
