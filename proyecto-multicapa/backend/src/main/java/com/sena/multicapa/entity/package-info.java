/**
 * Capa de entidades (Entity).
 * <p>
 * Clases anotadas con JPA ({@code @Entity}) que representan las tablas de la
 * base de datos MySQL y se mapean mediante Hibernate.
 */
package com.sena.multicapa.entity;
